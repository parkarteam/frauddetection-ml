#!/bin/bash
echo "Before assembling"
echo `pwd`
echo `ls -l`
echo 'ls -l /'
chmod -R g+w /microservice
echo 'ls -l /'
/s2i/bin/assemble
rc=$?

if [ $rc -eq 0 ]; then
    echo "After successful assembling"
else
    echo "After failed assembling"
fi

exit $rc